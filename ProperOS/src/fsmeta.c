#include "fsmeta.h"
#include "todo.h"

int walk_tree(const char *root, long *nfiles, long *ndirs) {
    (void)root;
    (void)nfiles;
    (void)ndirs;
    TODO("implement walk_tree");
}

long du_bytes(const char *root) {
    (void)root;
    TODO("implement du_bytes");
}

int is_same_file(const char *a, const char *b) {
    (void)a;
    (void)b;
    TODO("implement is_same_file");
}
